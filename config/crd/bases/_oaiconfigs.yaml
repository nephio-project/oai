---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: oaiconfigs.
spec:
  group: ""
  names:
    kind: OAIConfig
    listKind: OAIConfigList
    plural: oaiconfigs
    singular: oaiconfig
  scope: Namespaced
  versions:
  - name: ""
    schema:
      openAPIV3Schema:
        description: OAIConfig is the Schema for the OAIConfigs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OAIConfigSpec defines the desired state of OAIConfig
            properties:
              image:
                description: image defines the image location for the OAI NF
                type: string
            required:
            - image
            type: object
          status:
            description: OAIConfigStatus defines the observed state of OAIConfig
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
